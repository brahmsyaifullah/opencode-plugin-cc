{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'if command -v opencode &>/dev/null; then VER=$(opencode --version 2>/dev/null || echo unknown); echo \"✅ Opencode $VER ready\"; else echo \"⚠️  Opencode not found. Run /opencode:setup to install.\"; fi'"
          }
        ]
      }
    ]
  }
}
