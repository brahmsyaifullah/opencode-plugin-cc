{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'export PATH=\"$HOME/.opencode/bin:$PATH\"; if command -v opencode &>/dev/null; then echo \"Opencode $(opencode --version 2>/dev/null || echo unknown) ready — /opencode:delegate to hand off coding tasks\"; else echo \"Opencode not found. Run /opencode:setup to install.\"; fi'"
          }
        ]
      }
    ]
  }
}
